import React from 'react';

// Cursor component disabled due to performance issues
// Reverting to default browser cursor for optimal site responsiveness
const Cursor: React.FC = () => {
  return null;
};

export default Cursor;
